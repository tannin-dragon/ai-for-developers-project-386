# Адрес API по контракту. По умолчанию относительный путь /v1
# (в dev-режиме запросы проксируются Vite на VITE_PROXY_TARGET).
# Для работы с отдельно запущенным бэкендом укажите абсолютный URL,
# например VITE_API_URL=http://app.loc:8080/v1
VITE_API_URL=/v1